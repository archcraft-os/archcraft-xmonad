#!/usr/bin/env bash

# launch alacritty with alternate config
if [ "$1" == "-f" ]; then
	alacritty --class 'alacritty-float,alacritty-float' --config-file ~/.xmonad/alacritty/alacritty.yml
else
	alacritty --config-file ~/.xmonad/alacritty/alacritty.yml
fi
